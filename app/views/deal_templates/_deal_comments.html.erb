<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Comments

    <a data-toggle="collapse" href="#deal_comments_new_form">
      Add comment
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @deal.comments.each do |comment| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/comments/<%= comment.id %>">
          <%= comment.created_at %>
        </a>

        <%if current_user == comment.user %>
          <a href="/delete_comment_from_deal/<%= comment.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="deal_comments_new_form">
        <%= render partial: "deal_templates/deal_comments_new_form" %>
      </div>
    </div>

  </ul>
</div>
